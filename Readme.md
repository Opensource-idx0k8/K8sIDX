### New k8s IDE written in rust

## How to run
1. Prerequisites:
   - Install rust using rustup
   - To run the application in watch mode `cargo watch -x check -x test -x run`
   - To build the binary `cargo build`
